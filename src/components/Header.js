import React from 'react';

const Header = (appName, props) =>{
    return(
        <header className="App-header">
            <h1>{ appName }</h1>
        </header>  
    );
}

export default Header;